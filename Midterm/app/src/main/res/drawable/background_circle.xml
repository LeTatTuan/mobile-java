<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent"/>
    <stroke
        android:width="2dp"
        android:color="#EFE6E6"/> <!-- Chỉ định màu và độ rộng của viền (stroke) -->
    <size
        android:width="50dp"
        android:height="50dp"/> <!-- Kích thước của hình tròn -->
</shape>
